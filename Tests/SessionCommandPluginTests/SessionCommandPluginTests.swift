import XCTest
@testable import SessionCommandPlugin

final class SessionCommandPluginTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SessionCommandPlugin().text, "Hello, World!")
    }
}
